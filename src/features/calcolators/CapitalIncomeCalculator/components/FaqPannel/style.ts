import { Row, Space } from "antd";
import styled from "styled-components";

export const RowStayled = styled(Row)`
  text-align: "start";
  padding: 0 16px;
`;

export const SpaceStayled = styled(Space)`
  padding-left: 24;
`;
